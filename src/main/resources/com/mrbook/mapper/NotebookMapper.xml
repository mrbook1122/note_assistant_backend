<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mrbook.mapper.NotebookMapper">
    <insert id="save" useGeneratedKeys="true" keyProperty="id">
        insert into notebook (update_time, status, name, user_id)
        values (#{updateTime}, #{status}, #{name}, #{userID})
    </insert>
    <delete id="delete">
        delete
        from notebook
        where id = #{id}
    </delete>
    <select id="getAllNotebooksByUser" resultType="notebook">
        select *
        from notebook
        where user_id = #{id}
    </select>
    <select id="getNotebooksByUser" resultType="notebook">
        select *
        from notebook
        where user_id = #{id}
          and status = 1
    </select>
    <select id="getDeleteNotebooksByUser" resultType="notebook">
        select *
        from notebook
        where user_id = #{id}
          and status = 0
    </select>
    <update id="update">
        update notebook
        set update_time = #{updateTime},
            status      = #{status},
            name        = #{name},
            user_id     = #{userID}
        where id = #{id}
    </update>
</mapper>